# Archived: backend

The `backend/` directory has been removed from this branch and archived here as a placeholder.

Why:
- The backend has been intentionally removed from the active branch to simplify the frontend-only distribution.

How to restore:
- Check other branches (or the repository history) for the full backend source if you need it.
- If you want the backend restored to this branch, open a PR with the original `backend/` files or contact the maintainers.

Note:
- This placeholder ensures tools and contributors see a clear, non-destructive archive location and avoids confusing missing-file errors.